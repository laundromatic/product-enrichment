// Stub — full tests in src/__tests__/schema-org.test.ts
import { describe, it, expect } from 'vitest';
describe('schema-org (stub)', () => {
  it('placeholder', () => { expect(true).toBe(true); });
});
