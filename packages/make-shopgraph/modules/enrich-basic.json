{
  "name": "enrichBasic",
  "label": "Enrich Product (Basic)",
  "description": "Extract basic product data using Schema.org only. Free tier: 500 calls/month, no API key required.",
  "connection": "api-key",
  "type": "action",
  "parameters": [
    {
      "name": "url",
      "type": "text",
      "label": "Product URL",
      "required": true,
      "help": "The URL of the product page to extract data from"
    },
    {
      "name": "strict_confidence_threshold",
      "type": "number",
      "label": "Confidence Threshold",
      "required": false,
      "help": "Fields below this confidence (0.0-1.0) will be scrubbed to null with explanation. Leave empty for all fields."
    },
    {
      "name": "format",
      "type": "select",
      "label": "Output Format",
      "required": false,
      "default": "default",
      "options": [
        { "label": "Default (ProductData)", "value": "default" },
        { "label": "UCP (Universal Commerce Protocol)", "value": "ucp" }
      ]
    },
    {
      "name": "include_score",
      "type": "boolean",
      "label": "Include AgentReady Score",
      "required": false,
      "default": false,
      "help": "Include agent-readiness score (0-100) in the response"
    }
  ],
  "request": {
    "url": "{{baseUrl}}/api/enrich/basic",
    "method": "POST",
    "headers": {
      "Content-Type": "application/json"
    },
    "body": {
      "url": "{{parameters.url}}",
      "strict_confidence_threshold": "{{parameters.strict_confidence_threshold}}",
      "format": "{{parameters.format}}",
      "include_score": "{{parameters.include_score}}"
    }
  },
  "response": {
    "output": "{{body}}"
  }
}
